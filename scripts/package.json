{
  "name": "vouch-scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vouch CLI integration scripts: day-0 throwaway tx, capability lifecycle smoke test, executor reference PTBs.",
  "scripts": {
    "lifecycle": "tsx lifecycle.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
